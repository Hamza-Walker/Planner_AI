
FROM python:3.11-slim

WORKDIR /app

RUN pip install fastapi uvicorn pydantic

# Copy source code
COPY src/ /app/src/

ENV PYTHONPATH=/app/src

# Run the price service script we created
CMD ["python", "src/api/price_service.py"]
